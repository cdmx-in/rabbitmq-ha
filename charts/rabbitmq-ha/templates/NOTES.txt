RabbitMQ cluster "{{ .Release.Name }}" ({{ .Values.replicas }} brokers, quorum queues by default).

Admin credentials (generated by the operator):
  kubectl -n {{ .Release.Namespace }} get secret {{ .Release.Name }}-default-user -o jsonpath='{.data.username}' | base64 -d
  kubectl -n {{ .Release.Namespace }} get secret {{ .Release.Name }}-default-user -o jsonpath='{.data.password}' | base64 -d

Endpoints (in-cluster): {{ .Release.Name }}.{{ .Release.Namespace }}.svc.cluster.local:5672 (AMQP), :15672 (management UI)
Status:                 kubectl -n {{ .Release.Namespace }} get rabbitmqcluster {{ .Release.Name }}
